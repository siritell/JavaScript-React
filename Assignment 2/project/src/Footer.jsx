// REVIEW: No CSS file is imported here. The .footer class styles live in App.css, which
// couples this component to App's stylesheet. Import a dedicated Footer.css (or move the
// styles here) so the component is self-contained and reusable.

function Footer() {
  return <footer className="footer">&copy; 2026 Siri Tell</footer>;
}

export default Footer;
